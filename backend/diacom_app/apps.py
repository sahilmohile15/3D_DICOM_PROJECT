"""Application configuration for the imaging backend app."""

from django.apps import AppConfig


class DiacomAppConfig(AppConfig):
    """Django application config for diacom_app."""
    name = 'diacom_app'
